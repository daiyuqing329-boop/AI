<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gongdijigong">

    <!-- 数据后续会上传服务器，声明网络权限（本地功能完全离线可用） -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.GongDiJiGong">

        <activity android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.ProjectsActivity" android:exported="false" />
        <activity android:name=".ui.RecordsActivity" android:exported="false" />
        <activity android:name=".ui.AddRecordActivity" android:exported="false" />
        <activity android:name=".ui.BorrowActivity" android:exported="false" />
        <activity android:name=".ui.CalendarActivity" android:exported="false" />
        <activity android:name=".ui.StatsActivity" android:exported="false" />
        <activity android:name=".ui.ExportActivity" android:exported="false" />
    </application>
</manifest>
